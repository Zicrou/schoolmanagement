<div class="d-flex justify-content-center">
  <%= form_with(model: classroom) do |form| %>
    <% if classroom.errors.any? %>
      <div style="color: red">
        <h2><%= pluralize(classroom.errors.count, "error") %> prohibited this classroom from being saved:</h2>

        <ul>
          <% classroom.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="form-group">
      <%= form.label :nom, style: "display: block" %>
      <%= form.text_field :nom, class:"form-control md-4"  %>
    </div>

    <div>
      <%= form.label :niveau_id, style: "display: block" %>
      <%= form.collection_select :niveau_id, Niveau.order(:id), :id, :nom, class:"form-control md-4"  %>
    </div>

    <div class="form-group mt-2">
      <%= form.submit "Valider", class:"btn btn-primary col-md-4" %>
    </div>
  <% end %>
</div>